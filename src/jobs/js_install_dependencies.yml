description: >
  Install dependencies
executor:
  name: js
steps:
  - checkout
  - run:
      name: Install dependencies
      command: yarn --immutable
  - persist_to_workspace:
      root: .
      paths:
        - node_modules
