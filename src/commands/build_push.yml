description: >
  todo

parameters:
  path:
    description: 'Path to directory containing isopod.yml file'
    type: string
    default: '.'

steps:
  - run:
      name: 'Build the Docker image'
      command: isopod -f << parameters.path >>/isopod.yml build
