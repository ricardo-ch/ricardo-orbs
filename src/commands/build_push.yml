description: >
  todo

parameters:
  path:
    description: 'Path to directory containing isopod.yml file'
    type: string

steps:
  - run:
      name: 'Build the Docker image'
      command: |
        echo << parameters.path >>/isopod.yml
        isopod -f << parameters.path >>/isopod.yml build
